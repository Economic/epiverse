# epiverse 0.1.0

* Initial release.
* Attaches epidatatools, epiextractr, epitargets, prefab, realtalk, and swadlr.
* Startup message with package versions and conflict detection.
* `epiverse_packages()` lists all included packages.
* `epiverse_conflicts()` reports function conflicts with other loaded packages.
